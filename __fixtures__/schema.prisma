datasource db {
  provider = "sqlite"
  url      = "file:./db.sqlite3"
}

// model Report {
//   id          String  @id @default(uuid())
//   notes       String
//   isPublished Boolean @default(true)
// }

// model User {
//   firstName String
//   lastName  String
//   createdAt DateTime @default(now())

//   @@id([firstName, lastName])
// }

model Test {
  id       Int     @unique
  name     String
  result   String?
  user     User?   @relation(fields: [userName], references: [name], onUpdate: NoAction, onDelete: Cascade)
  userName String?
}

model User {
  name String @id
  Test Test[]
}
